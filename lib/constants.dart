import 'package:flutter/material.dart';

// My constants
const bottomContainerHeight = 60.0;
const activeReusableContainercolor = Color(0XFF1D1E33);
const bottomContainercolor = Color(0xFF006064);
const inactiveReusableContainercolor = Color(0XFF111328);

// Gender enums

enum EnumGender {
  male,
  female,
}

// title textstyle
const kTitleTextStyle = TextStyle(
    fontSize: 30.0, fontWeight: FontWeight.bold, color: Color(0xFF24D867));

const kResultTextStyle = TextStyle(
    fontSize: 40.0, fontWeight: FontWeight.bold, color: Color(0xFF24D867));

const kBmiTextStyle = TextStyle(
    fontSize: 100.0, fontWeight: FontWeight.bold, color: Colors.white);

const kBodyTextStyle = TextStyle(fontSize: 22.0);
